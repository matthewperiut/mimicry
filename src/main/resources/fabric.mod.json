{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": ["${authors}"],
  "contact": {
    "sources": "${sources_url}"
  },
  "license": "${license}",
  "icon": "assets/mimicry/icon.png",
  "environment": "*",
  "entrypoints": {},
  "mixins": [
    "${id}.mixins.json"
  ],
  "depends": {}
}
