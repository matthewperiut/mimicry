modLoader = "javafml"
loaderVersion = "[47,)"
license = "${license}"

[[mods]]
modId = "${id}"
version = "${version}"
displayName = "${name}"
authors = "${authors}"
description = "${description}"
logoFile = "assets/mimicry/icon.png"
displayURL = "${sources_url}"

${dependencies}
